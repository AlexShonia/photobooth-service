services:
  booth-fastapi:
    container_name: booth-fastapi
    build: 
      context: ./
      dockerfile: Dockerfile
    command: uvicorn main:app --host 0.0.0.0
    ports:
      - "0.0.0.0:8011:8000"
